%h1 Données


- if @sources.any?
  %table.sources
    %tr
      %th
        Données
      %th.created_at
        Ajouté le
      %th.actions
        Actions

    - @sources.each do |source|
      %tr
        %td.label
          = link_to source.label, source
        %td.date
          = l source.created_at
        %td.actions
          %ul.actions
            %li= link_to 'Preview', source_preview_path(source)
            %li= link_to 'Télécharger', source_download_path(source)
            %li= link_to 'Modifier', edit_source_path(source)
            %li= link_to 'Supprimer', source, method: :delete, data: { confirm: 'Confirmez la suppression' }

- else
  %p Aucune donnée disponible.


%ul.actions
  %li
    = link_to 'Ajouter des données', new_source_path
