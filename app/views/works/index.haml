%h1 Analyses en cours


- if @works.any?
  %table
    %thead
      %tr
        %th
          Analyse
        %th
          Fichier
        %th.created_at
          Démarrée le

    %tbody
      - @works.each do |work|
        %tr
          %td
            = work.operation.name
          %td
            = work.source.label
          %td.created_at
            = work.created_at
