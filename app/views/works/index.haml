%h1 Analyses


- if @works.any?
  %table
    %thead
      %tr
        %th
          Fichier
        %th
          Analyse
        %th.date
          Demande
        %th.date
          Traitement
        %th.date
          Fin

    %tbody
      - @works.each do |work|
        %tr
          %td
            = work.source.label
          %td
            = work.operation.name
          %td.date
            = work.created_at
          %td.date
            = work.started_at || 'N/A'
          %td.date
            = work.processed_at || 'N/A'
